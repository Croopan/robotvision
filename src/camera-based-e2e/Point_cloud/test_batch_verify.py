import torch
import cv2
import numpy as np

# Just checking the source code manually using awk to see if there's any print we could add.
# Actually I'll just look at the python execution from earlier. It succeeded and the tensor size was verified in test_batch2.py.
